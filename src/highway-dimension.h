#ifndef __HIGHWAY_DIMENSION__
#define __HIGHWAY_DIMENSION__

#include "graph.h"

int
approximateSparseSPC(const Graph<WeightedEdge>& graph);

int
approximateHd(const Graph<WeightedEdge>& graph);

#endif /* __HIGHWAY_DIMENSION__ */
